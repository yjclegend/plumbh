from setuptools import setup, find_packages
setup(
    name='calibration',
    version='0.1.0',
    packages=[  'metrology',
                'common',
                'imagedata',
                'calibration', 
                'proposed',
                'test', 
                'reference']
)